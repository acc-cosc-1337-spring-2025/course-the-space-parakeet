#include "decimals.h"
using std::cout;

int main() {
    cout << "Double (decimal) comparisons not reliable: ";
    double result - add_to_doble(0);
    cout << "Output: " << result << "\n";
    
    return 0;
}